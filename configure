#! /bin/bash

mkdir -p ~/schedule-data/auth/root
mkdir -p ~/schedule-data/auth/usr
mkdir -p ~/schedule-data/public/school
mkdir -p ~/schedule-data/usr
mkdir -p ~/schedule-data/root

echo true > ~/schedule-data/init